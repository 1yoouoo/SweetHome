export type signUpType = {
  email: string;
  name: string;
  nickName: string;
  password: string;
};

// const a = 1
// { a } => "{ a: 1 }"

// function test1(foo: string, bar: number) {

// }

// test1("1", 2)

// function test2({foo, bar}: {foo:string, bar: number}) {

// }

// test2({foo: "a", bar: 2})
